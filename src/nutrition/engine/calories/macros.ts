// Macro grams used by every calorie method. Pulled from recipe totals (grams/serving).

export interface MacroGrams {
  protein: number;
  totalCarbohydrate: number;
  totalFat: number;
  dietaryFiber: number;
  solubleFiber: number;
}
